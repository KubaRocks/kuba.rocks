import styled from "styled-components";

export const ContactDetailsStyled = styled.div`
  display: grid;
  .icons {
    font-size: 3rem;
    margin-left: -1rem;
  }
`;

export const ContactDetailItemStyles = styled.div`
  display: grid;
  grid-template-columns: 4rem 1fr;
`;
